@{
    ViewData["Title"] = "No Results";
}

<div class="container text-center">
    <img src="..\images\NoEmployeesResults.png" alt="No Results" class="img-fluid no-results-image mt-4 mx-auto d-block" />
    <br />
    <a href="@Url.Action("Index", "Employees")" class="btn btn-primary">Back to Employees</a>
</div>

<style>
    /* Custom Styles */
    h1 {
        margin-bottom: 20px;
        margin-top: 20px;
    }

    .no-results-image {
        max-width: 50%; /* Ensure image does not exceed the container width */
        height: auto;
        border: 2px solid #ccc; /* Adds a border */
        border-radius: 10px; /* Rounded corners */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
</style>
