SELECT Name, VisitorId 
FROM Visitor
WHERE Name LIKE 'A%';

/* This query dispalys the columns, Name and VisitorId from the
Visitor table. It then finds particular Visitors who have a name
with "A" */
